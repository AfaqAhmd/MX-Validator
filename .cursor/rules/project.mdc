---
description: Core project configuration and commands for Lead List MX Validator
alwaysApply: true
---

# Lead List MX Validator

Next.js application for validating email deliverability via MX record checks.

## Tech Stack

- **Next.js 16** (App Router)
- **TypeScript** (strict mode)
- **PostgreSQL + Drizzle ORM**
- **Tailwind CSS v4**
- **Recharts** (analytics)
- **Ultracite + Biome** (linting)

## Commands

```bash
pnpm dev          # Development server
pnpm build        # Production build
pnpm check        # Lint/format check
pnpm fix          # Auto-fix issues
pnpm typecheck    # Type checking
pnpm db:push      # Push schema
pnpm db:generate  # Generate migrations
pnpm db:studio    # Drizzle Studio
pnpm test         # Playwright tests
```

## Key Paths

- Schema: `src/lib/db/schema.ts`
- UI Components: `src/components/ui/`
- API Routes: `src/app/api/`
